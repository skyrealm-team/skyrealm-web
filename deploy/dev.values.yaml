replicaCount: 2

ingress:
  hosts:
    - host: dev.skyrealm.ai
      paths:
        - path: /
          pathType: ImplementationSpecific

env:
  next_public_backend_api: https://dev.skyrealm.ai/gql/schema
