export enum Variant {
  text = 'text',
  outlined = 'outlined',
  contained = 'contained',
}

export enum Color {
  primary = 'primary',
  secondary = 'secondary',
}

export enum Size {
  sm = 'small',
  md = 'medium',
  lg = 'large',
}

export enum Severity {
  error = 'error',
  warning = 'warning',
  info = 'info',
  success = 'success',
}
